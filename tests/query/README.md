# tests/query

Tests for `src/query/`.

## Files

- **`engine.test.ts`** — Tests for `QueryEngine.run` (schema filtering, condition operators, wikilink resolution, sorting, and limit) and `parseQueryString` (attribute-style query parsing).
